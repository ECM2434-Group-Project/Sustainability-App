import React, { useEffect, useState } from "react";
import { AnswerButton } from "../../components/Quiz/AnswerButton";
import { NextQuizButton } from "../../components/Quiz/NextQuizButton";
import { GoBackLink } from "../../components/General/GoBackLink";
import { Link } from "react-router-dom";
/**
 * the quiz page, shows the user with questions generated by the backend
 * allows the user to answer the questions and submit them to the backend
 * 
 * @returns a quiz page
 */
export default function Quiz() {

    // TODO: Remove with the backend questions, restructure to adapt to the backend
    const [questions, setQuestions] = useState([{
        question: "What is the capital of France?",
        answers: [
            "Paris",
            "London",
            "Berlin",
            "Madrid"
        ]
    },
    {
        question: "How old is Wiktor",
        answers: [
            "18",
            "19",
            "20",
            "21"
        ]
    }])

    // state to keep track of if the user has selected an answer
    const [selected, setSelected] = useState(false)

    // which question is the user on
    const [page, setPage] = useState(0)

    // function to check if the answer is correct
    const checkCorrect = () => {
        console.log("checking if correct")

        // check if the answer is correct
        const correct = true;

        // if correct, set the page to the next question
        if (correct) {
            setPage((page) => page + 1)
        }
    }

    // function to collect the bag after the quiz is complete
    const collectBag = () => {

		// Collect the bag
		console.log("Bag collected")
	}

    // on load, get questions from the backend
    useEffect(() => {
        setSelected(false)
    }, [page])

    // return the quiz, selecting first question or second question based on the page state
    return (
        <div className="w-full h-full flex flex-col">
            {
                page === 0 ? (
                    <>
                        <GoBackLink href="/outlet" />

                        <div className="w-full h-full flex flex-col items-center justify-center">
                            <h className="font-extrabold">Welcome To Our Sustainability Quiz</h>
                            <p className="w-3/4 pt-10">Our sustainability quiz aims to inspire and inform the student population about ways in which they can become more <small className="text-exeterBrightGreen font-bold">environmentally friendly</small> among spreading awareness about how we behave.</p>
                            <p className="w-3/4 pt-5">To proceed to the quiz please press the next button. There will be a total of 2 questions and you will receive the bag if you finish the quiz before <small className="text-exeterBrightGreen font-bold">anyone else.</small></p>
                            <NextQuizButton callback={() => {
                                setPage((page) => page + 1)
                            }}/>
                        </div>
                    </>
                    
                ) : (
                    <>
                        {
                            page === 1 ? (
                                <div className="flex flex-col items-center pt-20">
                                    <h1 className="text-2xl font-semibold">Quiz</h1>
                                    <p>{questions[0] ? questions[0].question : "not yet loaded in"}</p>

                                    {
                                        questions[0] ? (
                                            questions[0].answers.map((answer, index) => (
                                                <AnswerButton key={index} answer={answer} setSelected={setSelected} />
                                            ))
                                        ) : (
                                            <>not loaded in yet</>
                                        )
                                    }

                                    <NextQuizButton disabled={!selected} callback={checkCorrect}/>
                                    
                                </div>
                            ) : (
                                <>
                                    {
                                        page === 2 ? (
                                            <div className="flex flex-col items-center pt-20">
                                                <h1 className="text-2xl font-semibold">Quiz</h1>
                                                <p>{questions[1] ? questions[1].question : "not yet loaded in"}</p>

                                                {
                                                    questions[1] ? (
                                                        questions[1].answers.map((answer, index) => (
                                                            <AnswerButton key={index} answer={answer} setSelected={setSelected} />
                                                        ))
                                                    ) : (
                                                        <>not loaded in yet</>
                                                    )
                                                }
                                                
                                                <NextQuizButton disabled={!selected} callback={checkCorrect}/>
                                                    
                                            </div>
                                        ) : (
                                            <div className="w-full h-full flex flex-col items-center justify-center">
                                                <h className="font-extrabold">Thank You For Completing Our Sustainability Quiz</h>
                                                <p className="w-3/4 pt-10">Our sustainability quiz aims to inspire and inform the student population about ways in which they can become more <small className="text-exeterBrightGreen font-bold">environmentally friendly</small> among spreading awareness about how we behave.</p>
                                                <p className="w-3/4 pt-5">To see if you got your mystery bag please <small className="text-exeterBrightGreen font-bold">visit your orders section</small> using the button below.</p>
                                                
                                                <Link className="rounded-full p-2 text-center m-1 border mr-32 ml-32 hover:bg-exeterHighlightGreen" to={"/orders"}>
                                                    Your Orders
                                                </Link>
                                            </div>
                                        )
                                    }
                                </>
                                
                            )
                        }
                    </>
                )
            }
        </div>
    )
}